# Secrets for the staging destination.
# Loaded automatically when running: KAMAL_DESTINATION=staging bin/kamal deploy

# Same registry password as production (same Docker Hub account)
KAMAL_REGISTRY_PASSWORD=$KAMAL_REGISTRY_PASSWORD

# Use the same master key as production, or a dedicated staging key
RAILS_MASTER_KEY=$(cat config/master.key)
